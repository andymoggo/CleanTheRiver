const ProjectManager = require('./projectManager');

//Direction
let project = "Clean the river of the town";

let manager = new ProjectManager("Marc Andreesen", true);
manager.planningProject(project);